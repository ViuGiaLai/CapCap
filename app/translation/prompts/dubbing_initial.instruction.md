[mode=dubbing_rewrite][action=translate_for_dubbing] Create short spoken text in the requested target language for dubbing. Use `duration`, `max_words_vi` (legacy field name: target-language word budget), and `speech_cost` as hard constraints. Return one concise spoken line that is shorter than subtitle-style wording when needed. Preserve names, numbers, products, and key claims exactly.{{style_clause}}
